package com.cursoIntegrador.lePettiteCoffe;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
/**
 * Pruebas de integración mínimas de la aplicación para verificar que el contexto de Spring Boot se carga correctamente.
 */
class LePettiteCoffeApplicationTests {

	@Test
	/**
	 * Comprueba que la aplicación se inicializa y el contexto de Spring se carga sin errores.
	 *
	 * @return void (indica que no devuelve ningún valor, la prueba falla si hay excepción durante el arranque)
	 */
	void contextLoads() {
	}

}
